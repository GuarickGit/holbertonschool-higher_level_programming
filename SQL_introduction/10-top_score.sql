-- Script that lists all records from second_table ordered by score (top first)
-- Selecting score and name, ordered by highest score first
SELECT score, name FROM second_table ORDER BY score DESC, name
